"use client";

import * as React from "react";
import AuthSkeletonLayout from "@/src/components/auth/AuthSkeletonLayout";

export default function RegisterCardSkeleton() {
  return <AuthSkeletonLayout mode="register" />;
}
