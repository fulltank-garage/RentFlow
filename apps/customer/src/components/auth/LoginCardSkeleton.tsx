"use client";

import * as React from "react";
import AuthSkeletonLayout from "@/src/components/auth/AuthSkeletonLayout";

export default function LoginCardSkeleton() {
  return <AuthSkeletonLayout mode="login" />;
}
